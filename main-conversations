#!/usr/bin/env node

const program = require("commander")

program
    .version("0.0.1")
    .command("list", "List conversations")
    .command("get <conversationId>", "Show a conversation")
    .parse(process.argv);
